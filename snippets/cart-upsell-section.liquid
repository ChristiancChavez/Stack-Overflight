{%- comment -%}
  This snippet is a placeholder for cart upsell.
  The actual cart-upsell section is rendered automatically
  by Shopify when it's included in templates/cart.json.
  
  For the cart drawer, we'll include the section content directly
  by rendering the section file as a snippet.
{%- endcomment -%}

{%- comment -%}
  Note: In Shopify, sections are automatically rendered when added to templates.
  For the cart page, the section is already in templates/cart.json.
  For the cart drawer, we can't easily access sections, so we'll skip it there
  or implement a simpler version if needed.
{%- endcomment -%}

